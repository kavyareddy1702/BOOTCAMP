#include<stdio.h>
int main(){
    int n,array[100];
    printf("Enter the number of elements in the array : ");
    scanf("%d",&n);
    for(int i=1; i<=n; i++){
        scanf("%d",&array[i]);
    }
    int high = array[0];
    int low = array[0];
    for(int i=1; i<=n; i++)
    {
        

    }
}